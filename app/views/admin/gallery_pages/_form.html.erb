<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
  <h3 id="myModalLabel">Gallery Page</h3>
</div>

<%= form_for [:admin, @site, @gallery_page] do |f| %>
<div class="modal-body">
  <% if @gallery_page.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@gallery_page.errors.count, "error") %> prohibited this page from being saved:</h2>

    <ul>
      <% @gallery_page.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
</div>
 Add some dropdown for the gallery to associate with.
<div class="modal-footer">
  <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
  <%= f.submit 'Submit', :class => 'btn btn-primary' %>
</div>

<% end %>

