<h1>Listing images</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @images.each do |image| %>
  <tr>
    <td><%= link_to 'Show', admin_site_image_path(@site, image) %></td>
    <td><%= link_to 'Edit', edit_admin_site_image_path(@site, image) %></td>
    <td><%= link_to 'Destroy', admin_site_image_path(@site, image), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Image', new_admin_site_image_path %>
