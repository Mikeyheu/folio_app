<h1>Listing galleries</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @galleries.each do |gallery| %>
  <tr>
    <td><%= gallery.name %></td>
    <td><%= link_to 'Show', site_gallery_path(@site, gallery) %></td>
    <td><%= link_to 'Edit', edit_site_gallery_path(@site, gallery) %></td>
    <td><%= link_to 'Destroy', site_gallery_path(@site, gallery), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gallery', new_site_gallery_path %>
