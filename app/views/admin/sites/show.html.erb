<p id="notice"><%= notice %></p>

<table>
<% @site.galleries.each do |gallery| %>
	<tr>
    <td><%= gallery.name %> | </td>
    <td><%= link_to 'Show', admin_site_gallery_path(@site, gallery) %></td>
    <td><%= link_to 'Edit', edit_admin_site_gallery_path(@site, gallery) %></td>
    <td><%= link_to 'Destroy', admin_site_gallery_path(@site, gallery), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New Gallery', new_admin_site_gallery_path(@site) %>

<%= link_to 'Edit', edit_admin_site_path(@site) %> |
<%= link_to 'Back', admin_sites_path %>
